# 🎠 Руководство по слайдеру галереи

## Что это?

Первая карточка галереи "Summer Dress" теперь имеет интерактивный слайдер, позволяющий просматривать несколько фотографий одного платья прямо в окне карточки.

## ✨ Возможности

### Навигация
- **Стрелки влево/вправо** - появляются при наведении
- **Точки-индикаторы** - внизу карточки, показывают текущее фото
- **Свайп на мобильных** - проведите пальцем влево/вправо
- **Клавиатура** - стрелки ← → для переключения

### Интерактивность
- При наведении:
  - ✅ Появляются стрелки навигации
  - ✅ Появляются точки-индикаторы
  - ✅ Появляется оверлей с названием
  - ✅ Кнопка Instagram остается кликабельной

## 📁 Как добавить свои фотографии

### Вариант 1: Использовать папку dress1 (уже настроено)

Файлы уже подключены из `dress1/`:
```
dress1/
├── 1.jpg
├── 2.jpg
├── 3.jpg
├── 4.jpg
├── 5.jpg
└── 6.jpg
```

Просто замените эти файлы на свои фотографии с теми же именами!

### Вариант 2: Изменить пути в HTML

В `index.html` найдите секцию со слайдером:

```html
<div class="slider-image active" style="background-image: url('dress1/1.jpg');"></div>
<div class="slider-image" style="background-image: url('dress1/2.jpg');"></div>
```

Измените пути на свои:

```html
<div class="slider-image active" style="background-image: url('images/mydress-1.jpg');"></div>
<div class="slider-image" style="background-image: url('images/mydress-2.jpg');"></div>
```

## 🎨 Как добавить слайдер к другим карточкам

### Шаг 1: Создайте папку с фотографиями

Например, для второй карточки:
```
dress2/
├── 1.jpg
├── 2.jpg
└── 3.jpg
```

### Шаг 2: Скопируйте HTML структуру

В `index.html` найдите вторую карточку галереи:

```html
<div class="gallery-item">
    <div class="image-placeholder gallery-img-2"></div>
    <div class="gallery-overlay">
        <h4 data-en="Princess Gown" data-uk="Плаття Принцеси">Princess Gown</h4>
        <a href="https://instagram.com/avelle" target="_blank" class="gallery-link">
            <i class="fab fa-instagram"></i>
        </a>
    </div>
</div>
```

Замените на:

```html
<div class="gallery-item gallery-slider-item">
    <div class="gallery-slider">
        <div class="slider-images">
            <div class="slider-image active" style="background-image: url('dress2/1.jpg');"></div>
            <div class="slider-image" style="background-image: url('dress2/2.jpg');"></div>
            <div class="slider-image" style="background-image: url('dress2/3.jpg');"></div>
        </div>
        <button class="slider-btn prev-btn" aria-label="Previous">
            <i class="fas fa-chevron-left"></i>
        </button>
        <button class="slider-btn next-btn" aria-label="Next">
            <i class="fas fa-chevron-right"></i>
        </button>
        <div class="slider-dots"></div>
    </div>
    <div class="gallery-overlay">
        <h4 data-en="Princess Gown" data-uk="Плаття Принцеси">Princess Gown</h4>
        <a href="https://instagram.com/avelle" target="_blank" class="gallery-link">
            <i class="fab fa-instagram"></i>
        </a>
    </div>
</div>
```

**Готово!** JavaScript автоматически подключится ко всем слайдерам.

## ⚙️ Настройки

### Включить автоплей

В `script.js` найдите секцию "Auto-play" и раскомментируйте код:

```javascript
// Раскомментируйте эти строки для автоплея
let autoplayInterval = setInterval(nextSlide, 3000);

slider.addEventListener('mouseenter', () => {
    clearInterval(autoplayInterval);
});

slider.addEventListener('mouseleave', () => {
    autoplayInterval = setInterval(nextSlide, 3000);
});
```

### Изменить скорость переключения

В `style.css` найдите:

```css
.slider-image {
    transition: opacity 0.5s ease;  /* Измените 0.5s на нужное значение */
}
```

### Изменить цвет кнопок

В `style.css`:

```css
.slider-btn {
    background: rgba(255, 255, 255, 0.9);  /* Фон кнопок */
    color: var(--color-dark);              /* Цвет иконок */
}
```

### Изменить размер точек

В `style.css`:

```css
.slider-dot {
    width: 8px;   /* Размер неактивных точек */
    height: 8px;
}

.slider-dot.active {
    width: 10px;  /* Размер активной точки */
    height: 10px;
}
```

## 🎯 Рекомендации по фотографиям

### Формат и размер
- **Формат**: JPG или PNG
- **Размер**: 600×800px (или пропорции 3:4)
- **Качество**: Высокое, но оптимизированное для веба
- **Вес**: До 500KB на фото

### Количество фото
- **Минимум**: 2 фото
- **Оптимально**: 4-6 фото
- **Максимум**: Без ограничений (но лучше не более 10)

### Композиция
- Показывайте платье с разных ракурсов
- Включите детали (вышивка, ткань, аксессуары)
- Добавьте фото в контексте (на ребенке, на вешалке)
- Используйте единый стиль фотографий

## 🐛 Решение проблем

### Фотографии не отображаются

1. Проверьте пути к файлам в HTML
2. Убедитесь, что файлы находятся в правильной папке
3. Проверьте расширения файлов (.jpg, .jpeg, .png)

### Стрелки не работают

1. Очистите кэш браузера (Ctrl+Shift+R)
2. Проверьте консоль браузера на ошибки (F12)
3. Убедитесь, что подключен Font Awesome для иконок

### Слайдер не переключается на мобильных

Убедитесь, что JavaScript включен. Свайпы работают на всех современных мобильных браузерах.

### Overlay закрывает кнопки

В `style.css` проверьте z-index:
```css
.slider-btn {
    z-index: 10;  /* Должен быть больше, чем у overlay */
}

.gallery-overlay {
    z-index: 5;
}
```

## 💡 Полезные советы

1. **Оптимизируйте фото** перед загрузкой (используйте TinyPNG или Squoosh)
2. **Используйте одинаковые пропорции** для всех фото в слайдере
3. **Не делайте слайдеры для всех карточек** - оставьте некоторые статичными для разнообразия
4. **Тестируйте на мобильных** - убедитесь, что свайпы работают корректно

---

**Создано для Avellé** 🌟

Если у вас возникли вопросы, проверьте основной README.md или QUICKSTART.md

